{
  "name": "8-restweb",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "dev": "tsnd --respawn --clear src/app.ts",
    "start": "npm run build && npm run prisma:migrate:prod && node dist/app.js",
    "prisma:migrate:prod": "prisma migrate deploy && prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.7.2",
    "prisma": "^6.18.0",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@prisma/client": "^6.18.0",
    "dotenv": "^17.2.3",
    "env-var": "^7.5.0",
    "express": "^5.1.0"
  }
}
